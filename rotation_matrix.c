#include<stdio.h>

void shift(int a[],int rot)
{
	int i,temp;
	for(i=0;i<rot;i++)
	{
		temp=a[0];
		a[0]=a[n]
	}
}

int main()
{
	int a[20],i,n,rot;
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	printf("Enter the number of times you want to rotate");
	scanf("%d",&rot);
	shift(a,rot);
}
